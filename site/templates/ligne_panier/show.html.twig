<!DOCTYPE html>

<title>LignePanier</title>

{% block body %}
    <h1>LignePanier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_panier.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligne_panier_index') }}">back to list</a>

    <a href="{{ path('ligne_panier_edit', {'produit': ligne_panier.produit}) }}">edit</a>

    {{ include('ligne_panier/_delete_form.html.twig') }}
{% endblock %}
